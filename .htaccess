RewriteEngine On

# Redirect to login if not authenticated (for non-PHP files)
RewriteCond %{REQUEST_URI} !\.php$
RewriteCond %{REQUEST_URI} !\.(css|js|png|jpg|gif|ico|svg|woff|woff2|ttf|eot)$
RewriteRule ^(.*)$ index.php [L]

# Protect data directory
RewriteRule ^data/ - [F,L]
